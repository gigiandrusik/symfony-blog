<div class="x_content">
    <br>

    {{ form_start(form) }}

    <div class="form-group {{ form.category.vars.errors|length > 0 ? 'has-error' }}">

        {{ form_label(form.category) }}

        <div class="col-md-6 col-sm-6 col-xs-12">

            {{ form_widget(form.category) }}

            {% if form.category.vars.errors|length > 0 %}

                <span class="help-block">
                    {% for error in form.category.vars.errors %}
                        <strong>{{ error.message }}</strong>
                    {% endfor %}
                </span>
            {% endif %}
        </div>

    </div>

    <div class="form-group {{ form.name.vars.errors|length > 0 ? 'has-error' }}">

        {{ form_label(form.name) }}

        <div class="col-md-6 col-sm-6 col-xs-12">

            {{ form_widget(form.name) }}

            {% if form.name.vars.errors|length > 0 %}

                <span class="help-block">
                    {% for error in form.name.vars.errors %}
                        <strong>{{ error.message }}</strong>
                    {% endfor %}
                </span>
            {% endif %}
        </div>

    </div>

    <div class="form-group {{ form.content.vars.errors|length > 0 ? 'has-error' }}">

        {{ form_label(form.content) }}

        <div class="col-md-6 col-sm-6 col-xs-12">

            {{ form_widget(form.content) }}

            {% if form.content.vars.errors|length > 0 %}

                <span class="help-block">
                    {% for error in form.content.vars.errors %}
                        <strong>{{ error.message }}</strong>
                    {% endfor %}
                </span>
            {% endif %}
        </div>

    </div>

    <div class="form-group {{ form.file.vars.errors|length > 0 ? 'has-error' }}">

        {{ form_label(form.file) }}

        <div class="col-md-6 col-sm-6 col-xs-12">

            {{ form_widget(form.file) }}

            {% if form.file.vars.errors|length > 0 %}

                <span class="help-block">
                    {% for error in form.file.vars.errors %}
                        <strong>{{ error.message }}</strong>
                    {% endfor %}
                </span>
            {% endif %}
        </div>

    </div>

    <div class="ln_solid"></div>

    <div class="form-group">
        <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
            <button class="btn btn-danger" type="reset">Reset</button>
            <button type="submit" class="btn btn-success" formnovalidate>Submit</button>
        </div>
    </div>

    {{ form_end(form) }}

</div>